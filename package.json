{
	"scripts": {
		"dev": "concurrently --kill-others-on-fail 'node styles.js' 'nodemon -e ts,tsx -i \"**/*.style.ts\" -x \"ags run -d . --gtk 4\"'",
		"format:check": "prettier -c src",
		"format:write": "prettier -w src",
		"generate-types": "npm ci && ags types -d . -u"
	},
	"dependencies": {
		"ags": "*",
		"gnim": "*"
	},
	"prettier": {
		"semi": false,
		"tabWidth": 2
	},
	"devDependencies": {
		"concurrently": "^9.2.1",
		"postcss": "^8.5.6",
		"postcss-modules": "^6.0.1",
		"prettier": "^3.6.2",
		"sass": "^1.94.2",
		"watcher": "^2.3.1"
	},
	"volta": {
		"node": "24.11.1"
	}
}